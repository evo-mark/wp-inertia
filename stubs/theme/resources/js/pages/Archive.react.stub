import { Head, usePage } from "@inertiajs/react";

const Archive = () => {
    const { wp, archive } = usePage().props;
    return (
        <>
            <Head title={wp.name} />
            <div className="flex-centre">
                <h1>Welcome to {wp.name}</h1>
            </div>
        </>
    );
};

export default Archive;
